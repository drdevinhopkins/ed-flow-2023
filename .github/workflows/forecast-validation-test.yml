name: Forecast validation branch test

on:
  push:
    branches: ["forecast-validation"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: chronos-requirements.txt

      - name: Cache HuggingFace models
        uses: actions/cache@v5
        with:
          path: ~/.cache/huggingface
          key: huggingface-forecast-validation-${{ runner.os }}-${{ hashFiles('chronos-requirements.txt', 'scripts/backtest_covariate_ablation.py') }}
          restore-keys: |
            huggingface-forecast-validation-${{ runner.os }}-
            huggingface-chronos2-${{ runner.os }}-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install -r chronos-requirements.txt

      - name: Compile validation scripts
        run: |
          python -m py_compile scripts/validate_forecast_inputs.py
          python -m py_compile scripts/backtest_covariate_ablation.py

      - name: Validate live forecast inputs
        run: python scripts/validate_forecast_inputs.py

      - name: Smoke-test covariate ablation
        run: |
          mkdir -p validation-output
          python scripts/backtest_covariate_ablation.py \
            --horizon 6 \
            --num-cutoffs 1 \
            --max-history-days 30 \
            --min-history-hours 168 \
            --output-dir validation-output

      - name: Upload ablation results
        uses: actions/upload-artifact@v4
        with:
          name: covariate-ablation-smoke
          path: validation-output/*.csv
