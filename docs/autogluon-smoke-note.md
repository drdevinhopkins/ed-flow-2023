# AutoGluon smoke test

This branch includes a temporary GitHub Actions smoke test for the AutoGluon backtest harness. It runs a reduced one-window backtest on Python 3.12/CPU to catch dependency, API, schema, and runtime failures before testing on the production V100 server.
